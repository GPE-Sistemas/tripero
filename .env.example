# ============================================
# TRIPERO - GPS Trip Detection Microservice
# Environment Configuration
# ============================================

# --------------------------------------------
# Server Configuration
# --------------------------------------------
PORT=3001
NODE_ENV=production

# --------------------------------------------
# Redis Configuration (REQUIRED)
# --------------------------------------------
# Redis is used for real-time state management and pub/sub events
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=your_redis_password_here

# --------------------------------------------
# Database Configuration (REQUIRED)
# --------------------------------------------
# TimescaleDB/PostgreSQL connection
# The service will automatically create tables and configure TimescaleDB
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=tripero

# Enable SQL query logging (useful for debugging, disable in production)
DB_LOGGING=false

# ============================================
# That's it! No other configuration needed.
# ============================================
#
# The service will:
# ✅ Auto-create database tables on first run (TypeORM synchronize)
# ✅ Auto-configure TimescaleDB hypertables
# ✅ Auto-setup compression policies (7 days)
# ✅ Auto-setup retention policies (365 days)
#
# Just ensure you have:
# - A running Redis instance
# - A running TimescaleDB/PostgreSQL instance
# - The TimescaleDB extension enabled (if using TimescaleDB)
#
# Start command:
#   docker run -e DB_HOST=... -e REDIS_HOST=... tripero:latest
